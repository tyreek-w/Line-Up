
const types = ['creditCard', 'cash', 'bitcoin', ]

function paymentInfo (info){

  let payObj = {
    billingAddress: info.billingAddress,
    billingName: {
      firstName: info.firstName,
      lastName: info.lastName
    },
    type:
  }
}
